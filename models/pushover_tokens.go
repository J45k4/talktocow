// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PushoverToken is an object representing the database table.
type PushoverToken struct {
	ID        int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID    int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Token     string    `boil:"token" json:"token" toml:"token" yaml:"token"`
	UserToken string    `boil:"user_token" json:"user_token" toml:"user_token" yaml:"user_token"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *pushoverTokenR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L pushoverTokenL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PushoverTokenColumns = struct {
	ID        string
	UserID    string
	Token     string
	UserToken string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	UserID:    "user_id",
	Token:     "token",
	UserToken: "user_token",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

var PushoverTokenTableColumns = struct {
	ID        string
	UserID    string
	Token     string
	UserToken string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "pushover_tokens.id",
	UserID:    "pushover_tokens.user_id",
	Token:     "pushover_tokens.token",
	UserToken: "pushover_tokens.user_token",
	CreatedAt: "pushover_tokens.created_at",
	UpdatedAt: "pushover_tokens.updated_at",
}

// Generated where

var PushoverTokenWhere = struct {
	ID        whereHelperint
	UserID    whereHelperint
	Token     whereHelperstring
	UserToken whereHelperstring
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint{field: "\"pushover_tokens\".\"id\""},
	UserID:    whereHelperint{field: "\"pushover_tokens\".\"user_id\""},
	Token:     whereHelperstring{field: "\"pushover_tokens\".\"token\""},
	UserToken: whereHelperstring{field: "\"pushover_tokens\".\"user_token\""},
	CreatedAt: whereHelpertime_Time{field: "\"pushover_tokens\".\"created_at\""},
	UpdatedAt: whereHelpertime_Time{field: "\"pushover_tokens\".\"updated_at\""},
}

// PushoverTokenRels is where relationship names are stored.
var PushoverTokenRels = struct {
}{}

// pushoverTokenR is where relationships are stored.
type pushoverTokenR struct {
}

// NewStruct creates a new relationship struct
func (*pushoverTokenR) NewStruct() *pushoverTokenR {
	return &pushoverTokenR{}
}

// pushoverTokenL is where Load methods for each relationship are stored.
type pushoverTokenL struct{}

var (
	pushoverTokenAllColumns            = []string{"id", "user_id", "token", "user_token", "created_at", "updated_at"}
	pushoverTokenColumnsWithoutDefault = []string{"user_id", "token", "user_token"}
	pushoverTokenColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	pushoverTokenPrimaryKeyColumns     = []string{"id"}
)

type (
	// PushoverTokenSlice is an alias for a slice of pointers to PushoverToken.
	// This should almost always be used instead of []PushoverToken.
	PushoverTokenSlice []*PushoverToken
	// PushoverTokenHook is the signature for custom PushoverToken hook methods
	PushoverTokenHook func(context.Context, boil.ContextExecutor, *PushoverToken) error

	pushoverTokenQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	pushoverTokenType                 = reflect.TypeOf(&PushoverToken{})
	pushoverTokenMapping              = queries.MakeStructMapping(pushoverTokenType)
	pushoverTokenPrimaryKeyMapping, _ = queries.BindMapping(pushoverTokenType, pushoverTokenMapping, pushoverTokenPrimaryKeyColumns)
	pushoverTokenInsertCacheMut       sync.RWMutex
	pushoverTokenInsertCache          = make(map[string]insertCache)
	pushoverTokenUpdateCacheMut       sync.RWMutex
	pushoverTokenUpdateCache          = make(map[string]updateCache)
	pushoverTokenUpsertCacheMut       sync.RWMutex
	pushoverTokenUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var pushoverTokenBeforeInsertHooks []PushoverTokenHook
var pushoverTokenBeforeUpdateHooks []PushoverTokenHook
var pushoverTokenBeforeDeleteHooks []PushoverTokenHook
var pushoverTokenBeforeUpsertHooks []PushoverTokenHook

var pushoverTokenAfterInsertHooks []PushoverTokenHook
var pushoverTokenAfterSelectHooks []PushoverTokenHook
var pushoverTokenAfterUpdateHooks []PushoverTokenHook
var pushoverTokenAfterDeleteHooks []PushoverTokenHook
var pushoverTokenAfterUpsertHooks []PushoverTokenHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PushoverToken) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PushoverToken) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PushoverToken) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PushoverToken) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PushoverToken) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PushoverToken) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PushoverToken) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PushoverToken) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PushoverToken) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range pushoverTokenAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPushoverTokenHook registers your hook function for all future operations.
func AddPushoverTokenHook(hookPoint boil.HookPoint, pushoverTokenHook PushoverTokenHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		pushoverTokenBeforeInsertHooks = append(pushoverTokenBeforeInsertHooks, pushoverTokenHook)
	case boil.BeforeUpdateHook:
		pushoverTokenBeforeUpdateHooks = append(pushoverTokenBeforeUpdateHooks, pushoverTokenHook)
	case boil.BeforeDeleteHook:
		pushoverTokenBeforeDeleteHooks = append(pushoverTokenBeforeDeleteHooks, pushoverTokenHook)
	case boil.BeforeUpsertHook:
		pushoverTokenBeforeUpsertHooks = append(pushoverTokenBeforeUpsertHooks, pushoverTokenHook)
	case boil.AfterInsertHook:
		pushoverTokenAfterInsertHooks = append(pushoverTokenAfterInsertHooks, pushoverTokenHook)
	case boil.AfterSelectHook:
		pushoverTokenAfterSelectHooks = append(pushoverTokenAfterSelectHooks, pushoverTokenHook)
	case boil.AfterUpdateHook:
		pushoverTokenAfterUpdateHooks = append(pushoverTokenAfterUpdateHooks, pushoverTokenHook)
	case boil.AfterDeleteHook:
		pushoverTokenAfterDeleteHooks = append(pushoverTokenAfterDeleteHooks, pushoverTokenHook)
	case boil.AfterUpsertHook:
		pushoverTokenAfterUpsertHooks = append(pushoverTokenAfterUpsertHooks, pushoverTokenHook)
	}
}

// One returns a single pushoverToken record from the query.
func (q pushoverTokenQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PushoverToken, error) {
	o := &PushoverToken{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for pushover_tokens")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PushoverToken records from the query.
func (q pushoverTokenQuery) All(ctx context.Context, exec boil.ContextExecutor) (PushoverTokenSlice, error) {
	var o []*PushoverToken

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PushoverToken slice")
	}

	if len(pushoverTokenAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PushoverToken records in the query.
func (q pushoverTokenQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count pushover_tokens rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q pushoverTokenQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if pushover_tokens exists")
	}

	return count > 0, nil
}

// PushoverTokens retrieves all the records using an executor.
func PushoverTokens(mods ...qm.QueryMod) pushoverTokenQuery {
	mods = append(mods, qm.From("\"pushover_tokens\""))
	return pushoverTokenQuery{NewQuery(mods...)}
}

// FindPushoverToken retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPushoverToken(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*PushoverToken, error) {
	pushoverTokenObj := &PushoverToken{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"pushover_tokens\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, pushoverTokenObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from pushover_tokens")
	}

	if err = pushoverTokenObj.doAfterSelectHooks(ctx, exec); err != nil {
		return pushoverTokenObj, err
	}

	return pushoverTokenObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PushoverToken) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no pushover_tokens provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(pushoverTokenColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	pushoverTokenInsertCacheMut.RLock()
	cache, cached := pushoverTokenInsertCache[key]
	pushoverTokenInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			pushoverTokenAllColumns,
			pushoverTokenColumnsWithDefault,
			pushoverTokenColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(pushoverTokenType, pushoverTokenMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(pushoverTokenType, pushoverTokenMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"pushover_tokens\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"pushover_tokens\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into pushover_tokens")
	}

	if !cached {
		pushoverTokenInsertCacheMut.Lock()
		pushoverTokenInsertCache[key] = cache
		pushoverTokenInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PushoverToken.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PushoverToken) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	pushoverTokenUpdateCacheMut.RLock()
	cache, cached := pushoverTokenUpdateCache[key]
	pushoverTokenUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			pushoverTokenAllColumns,
			pushoverTokenPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update pushover_tokens, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"pushover_tokens\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, pushoverTokenPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(pushoverTokenType, pushoverTokenMapping, append(wl, pushoverTokenPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update pushover_tokens row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for pushover_tokens")
	}

	if !cached {
		pushoverTokenUpdateCacheMut.Lock()
		pushoverTokenUpdateCache[key] = cache
		pushoverTokenUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q pushoverTokenQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for pushover_tokens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for pushover_tokens")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PushoverTokenSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), pushoverTokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"pushover_tokens\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, pushoverTokenPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in pushoverToken slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all pushoverToken")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PushoverToken) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no pushover_tokens provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(pushoverTokenColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	pushoverTokenUpsertCacheMut.RLock()
	cache, cached := pushoverTokenUpsertCache[key]
	pushoverTokenUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			pushoverTokenAllColumns,
			pushoverTokenColumnsWithDefault,
			pushoverTokenColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			pushoverTokenAllColumns,
			pushoverTokenPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert pushover_tokens, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(pushoverTokenPrimaryKeyColumns))
			copy(conflict, pushoverTokenPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"pushover_tokens\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(pushoverTokenType, pushoverTokenMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(pushoverTokenType, pushoverTokenMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert pushover_tokens")
	}

	if !cached {
		pushoverTokenUpsertCacheMut.Lock()
		pushoverTokenUpsertCache[key] = cache
		pushoverTokenUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PushoverToken record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PushoverToken) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PushoverToken provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), pushoverTokenPrimaryKeyMapping)
	sql := "DELETE FROM \"pushover_tokens\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from pushover_tokens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for pushover_tokens")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q pushoverTokenQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no pushoverTokenQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from pushover_tokens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for pushover_tokens")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PushoverTokenSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(pushoverTokenBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), pushoverTokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"pushover_tokens\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, pushoverTokenPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from pushoverToken slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for pushover_tokens")
	}

	if len(pushoverTokenAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PushoverToken) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPushoverToken(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PushoverTokenSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PushoverTokenSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), pushoverTokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"pushover_tokens\".* FROM \"pushover_tokens\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, pushoverTokenPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PushoverTokenSlice")
	}

	*o = slice

	return nil
}

// PushoverTokenExists checks if the PushoverToken row exists.
func PushoverTokenExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"pushover_tokens\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if pushover_tokens exists")
	}

	return exists, nil
}
